{
  "name": "microflow",
  "version": "0.0.2",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "tsc --noEmit && jest",
    "set-version": "node scripts/set-version.js",
    "build": "rm -rf public/dist && npm run set-version && tsc",
    "build-watch": "rm -rf public/dist && npm run set-version && tsc --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ArnauNau/Microflow"
  },
  "author": "ArnauNau",
  "license": "LGPL-3.0-or-later",
  "description": "",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
